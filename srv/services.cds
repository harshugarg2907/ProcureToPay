// Production service structure marker.
// The active OData V4 service is currently defined in p2p-service.cds as P2PService.
// Keep this file as the canonical place to migrate service projections when the
// existing custom UI modules are replaced by generated Fiori Elements apps.
